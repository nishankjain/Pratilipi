{
  "name": "crashing-planes",
  "description": "A flight booking app",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "http-server": "^0.6.1",
    "bower": "^1.3.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -p 3000"
  }
}
